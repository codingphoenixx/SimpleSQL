plugins {
    id 'java'
    id 'maven-publish'

}

group = 'dev.coph'
version = '2.6.1'

repositories {
    maven {
        url = uri("https://repo.coph.dev/releases")
    }

    mavenCentral()
}

dependencies {
    implementation 'com.zaxxer:HikariCP:6.3.0'

    implementation "org.xerial:sqlite-jdbc:3.50.3.0"
    implementation 'com.mysql:mysql-connector-j:9.4.0'
    implementation 'org.mariadb.jdbc:mariadb-java-client:3.5.5'

    compileOnly 'org.projectlombok:lombok:1.18.38'
    annotationProcessor 'org.projectlombok:lombok:1.18.38'

    implementation "dev.coph:simplelogger:2.2.0"
    implementation "dev.coph:simpleutilities:3.5.0"
}

javadoc {
    options.encoding = 'UTF-8'
}


publishing {
    repositories {
        maven {
            name = "cophrepository"
            url = "https://repo.coph.dev/releases"
            credentials(PasswordCredentials)
            authentication {
                basic(BasicAuthentication)
            }
        }
    }
    publications {
        maven(MavenPublication) {
            groupId = "dev.coph"
            artifactId = "simplesql"
            version = this.version
            from components.java
        }
    }
}

jar {
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}

java {
    withSourcesJar()
    withJavadocJar()
}

javadoc {
    if(JavaVersion.current().isJava9Compatible()) {
        options.addBooleanOption('html5', true)
    }
}
